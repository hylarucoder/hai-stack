interface:
  display_name: "Hai Architecture"
  short_description: "Trace system complexity and review architecture"
  default_prompt: "Use $hai-architecture to investigate this system's change complexity or review its identified design boundary, using traced runtime and contract evidence."

policy:
  allow_implicit_invocation: true
