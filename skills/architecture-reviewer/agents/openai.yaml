interface:
  display_name: "Architecture Reviewer"
  short_description: "Architecture-level APoSD review"
  default_prompt: "Use $architecture-reviewer to review this architecture through the APoSD/Ousterhout lens."

policy:
  allow_implicit_invocation: true
